#To run the project, first install all the dependencies using:

npm install

#Run Command
npm run start

#This projected has been implemented mainly using react, redux and material-ui.

#Along with the search functionality, redux maintains a simple cahce for the last 5 search queries and thus optimises the performance.
